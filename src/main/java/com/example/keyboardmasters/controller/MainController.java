package com.example.keyboardmasters.controller;

import org.springframework.stereotype.Controller;

@Controller
public class MainController {

    // @GetMapping("/")
    // public RedirectView redirectToSpecificPage() {
    // return new RedirectView("/index"); // Replace "/welcome" with the desired
    // page URL
    // }
}
